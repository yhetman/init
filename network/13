# nslookup 10.51.1.253
# Server: 10.51.1.253
# Address:	10.51.1.253#53

# 253.1.51.10.in-addr.arpa	name = ns1.unit.ua.
# Type - Application Server (DNS || DHCP)

echo "nslookup for 10.51.1.253 :" && nslookup 10.51.1.253 && echo "\n" && echo "And now nslookup for host :" && nslookup host && echo "\n" && echo " 10.51.1.253 is a host that has DNS server type!\n Name of host : ns1.unit.ua\n"
